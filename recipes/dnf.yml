---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: dnf
repos:
  copr:
    - scottames/ghostty

install:
  packages:
    - ghostty
    - starship
    - git-lfs
    - sudo-rs
group-install:
  with-optional: true
  packages:
    - development-tools
    - virtualization
