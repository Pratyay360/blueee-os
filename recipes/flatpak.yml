---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
# image will be published to ghcr.io/pratyay360/blue-build
type: default-flatpaks
configurations:
  - notify: false
    scope: system
    repo:
      url: https://dl.flathub.org/repo/flathub.flatpakrepo
      name: flathub
      title: Flathub
    install:
      - org.gnome.Loupe
      - app.devsuite.Ptyxis
      - io.github.kolunmi.Bazaar
      - com.bitwarden.desktop
      - com.github.tchx84.Flatseal
      - io.github.flattool.Warehouse
      - io.podman_desktop.PodmanDesktop
      - io.github.sitraorg.sitra
      - com.rustdesk.RustDesk
      - io.github.nozwock.Packet
      - it.mijorus.gearlever
      - org.gnome.DejaDup
  # To enable the Orion beta remote, uncomment the block below AND add at
  # least one app under `install:`. An entry with only `repo:` and no
  # `install:`/`remove:` is invalid, so it stays commented out.
  # - notify: false
  #   scope: system
  #   repo:
  #     url: https://flatpak.orionbrowser.com/orion-beta.flatpakrepo
  #     name: orion-beta
  #     title: orion
  #   install:
  #     - com.kagi.Orion
